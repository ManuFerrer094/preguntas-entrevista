---
title: ¿Cómo implementar autenticación con Passport.js en Node.js?
difficulty: medium
tags: [nodejs, passport, authentication, oauth, jwt, express]
---

**Passport.js** es un middleware de autenticación para Node.js altamente configurable. Soporta más de 500 estrategias de autenticación (Local, JWT, OAuth con Google/GitHub/Facebook, etc.) con una API consistente.

## Instalación

```bash
npm install passport passport-local passport-jwt jsonwebtoken bcryptjs
npm install @types/passport @types/passport-local @types/passport-jwt  # TypeScript
```

## Estrategia Local (usuario/contraseña)

```javascript
// config/passport.js
import passport from 'passport';
import { Strategy as LocalStrategy } from 'passport-local';
import { Strategy as JwtStrategy, ExtractJwt } from 'passport-jwt';
import bcrypt from 'bcryptjs';
import { usuarioRepository } from '../repositories/usuario.repository.js';

const JWT_SECRET = process.env.JWT_SECRET;

// Estrategia Local: autenticación con email y contraseña
passport.use('local', new LocalStrategy(
  {
    usernameField: 'email',   // Nombre del campo del email
    passwordField: 'password'
  },
  async (email, password, done) => {
    try {
      const usuario = await usuarioRepository.findByEmail(email);

      if (!usuario) {
        return done(null, false, { message: 'Email no registrado' });
      }

      const passwordValida = await bcrypt.compare(password, usuario.passwordHash);
      if (!passwordValida) {
        return done(null, false, { message: 'Contraseña incorrecta' });
      }

      if (!usuario.activo) {
        return done(null, false, { message: 'Cuenta desactivada' });
      }

      // done(error, user, info)
      return done(null, usuario);
    } catch (error) {
      return done(error);
    }
  }
));

// Estrategia JWT: validación de tokens en requests autenticados
passport.use('jwt', new JwtStrategy(
  {
    jwtFromRequest: ExtractJwt.fromAuthHeaderAsBearerToken(),
    secretOrKey: JWT_SECRET,
    // ignoreExpiration: false (por defecto)
  },
  async (jwtPayload, done) => {
    try {
      const usuario = await usuarioRepository.findById(jwtPayload.sub);

      if (!usuario || !usuario.activo) {
        return done(null, false);
      }

      return done(null, usuario);
    } catch (error) {
      return done(error, false);
    }
  }
));

export default passport;
```

## Rutas de autenticación

```javascript
// routes/auth.routes.js
import { Router } from 'express';
import passport from 'passport';
import jwt from 'jsonwebtoken';
import bcrypt from 'bcryptjs';
import { usuarioRepository } from '../repositories/usuario.repository.js';

const router = Router();
const JWT_SECRET = process.env.JWT_SECRET;
const JWT_EXPIRY = process.env.JWT_EXPIRY ?? '24h';
const REFRESH_TOKEN_EXPIRY = '30d';

// Generar par de tokens (access + refresh)
function generarTokens(usuario) {
  const accessToken = jwt.sign(
    {
      sub: usuario.id,
      email: usuario.email,
      rol: usuario.rol
    },
    JWT_SECRET,
    { expiresIn: JWT_EXPIRY }
  );

  const refreshToken = jwt.sign(
    { sub: usuario.id, tipo: 'refresh' },
    JWT_SECRET,
    { expiresIn: REFRESH_TOKEN_EXPIRY }
  );

  return { accessToken, refreshToken };
}

// POST /auth/registro
router.post('/registro', async (req, res) => {
  try {
    const { nombre, email, password } = req.body;

    const existente = await usuarioRepository.findByEmail(email);
    if (existente) {
      return res.status(409).json({ error: 'El email ya está registrado' });
    }

    const passwordHash = await bcrypt.hash(password, 12);
    const usuario = await usuarioRepository.create({ nombre, email, passwordHash });
    const tokens = generarTokens(usuario);

    res.status(201).json({
      usuario: { id: usuario.id, nombre, email, rol: usuario.rol },
      ...tokens
    });
  } catch (error) {
    res.status(500).json({ error: 'Error en el servidor' });
  }
});

// POST /auth/login
router.post('/login',
  // Usar estrategia local sin crear sesión (stateless con JWT)
  passport.authenticate('local', { session: false }),
  (req, res) => {
    const tokens = generarTokens(req.user);

    res.json({
      usuario: {
        id: req.user.id,
        nombre: req.user.nombre,
        email: req.user.email,
        rol: req.user.rol
      },
      ...tokens
    });
  }
);

// POST /auth/refresh — Renovar access token
router.post('/refresh', async (req, res) => {
  const { refreshToken } = req.body;
  if (!refreshToken) return res.status(401).json({ error: 'Refresh token requerido' });

  try {
    const payload = jwt.verify(refreshToken, JWT_SECRET);
    if (payload.tipo !== 'refresh') throw new Error('Token inválido');

    const usuario = await usuarioRepository.findById(payload.sub);
    if (!usuario || !usuario.activo) return res.status(401).json({ error: 'Usuario no encontrado' });

    const accessToken = jwt.sign(
      { sub: usuario.id, email: usuario.email, rol: usuario.rol },
      JWT_SECRET,
      { expiresIn: JWT_EXPIRY }
    );

    res.json({ accessToken });
  } catch {
    res.status(401).json({ error: 'Refresh token inválido o expirado' });
  }
});

export default router;
```

## Middleware de protección de rutas

```javascript
// middleware/auth.middleware.js
import passport from 'passport';

// Middleware para rutas protegidas
export const autenticar = passport.authenticate('jwt', { session: false });

// Middleware de autorización por rol
export function autorizar(...rolesPermitidos) {
  return (req, res, next) => {
    if (!rolesPermitidos.includes(req.user?.rol)) {
      return res.status(403).json({ error: 'Acceso denegado. Permisos insuficientes.' });
    }
    next();
  };
}

// Uso en rutas:
router.get('/admin/usuarios',
  autenticar,                        // 1. Verificar JWT
  autorizar('admin', 'moderador'),   // 2. Verificar rol
  (req, res) => {
    res.json({ usuarios: [] });
  }
);
```

## Estrategia OAuth (Google)

```bash
npm install passport-google-oauth20
```

```javascript
import { Strategy as GoogleStrategy } from 'passport-google-oauth20';

passport.use('google', new GoogleStrategy(
  {
    clientID: process.env.GOOGLE_CLIENT_ID,
    clientSecret: process.env.GOOGLE_CLIENT_SECRET,
    callbackURL: '/auth/google/callback',
    scope: ['profile', 'email']
  },
  async (accessToken, refreshToken, profile, done) => {
    try {
      let usuario = await usuarioRepository.findByGoogleId(profile.id);

      if (!usuario) {
        usuario = await usuarioRepository.create({
          googleId: profile.id,
          nombre: profile.displayName,
          email: profile.emails[0].value,
          avatarUrl: profile.photos[0].value
        });
      }

      return done(null, usuario);
    } catch (error) {
      return done(error);
    }
  }
));

// Rutas OAuth
router.get('/auth/google', passport.authenticate('google'));
router.get('/auth/google/callback',
  passport.authenticate('google', { session: false }),
  (req, res) => {
    const { accessToken } = generarTokens(req.user);
    // Redirigir al frontend con el token
    res.redirect(`${process.env.FRONTEND_URL}/auth/callback?token=${accessToken}`);
  }
);
```

**Consejo de entrevista**: Passport.js es un middleware de autenticación, no una solución completa de autorización. En el patrón stateless con JWT, no se usan sesiones (`session: false`). Menciona la diferencia entre autenticación (¿quién eres?) y autorización (¿qué puedes hacer?), y el patrón de access token + refresh token: el access token es de corta duración (15-60 minutos) para seguridad, y el refresh token de larga duración (7-30 días) para comodidad del usuario. Almacena el refresh token en la base de datos para poder invalidarlo (logout, revocación).
