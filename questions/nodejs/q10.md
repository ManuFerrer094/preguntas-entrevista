---
title: ¿Qué es JWT y cómo implementarlo en Node.js?
difficulty: medium
tags: [nodejs, jwt, authentication, security]
---

JWT (JSON Web Token) es un estándar abierto (RFC 7519) para transmitir información de forma segura entre partes como un objeto JSON firmado digitalmente. Se usa ampliamente para autenticación y autorización en APIs REST, ya que es stateless: el servidor no necesita almacenar sesiones, toda la información está en el token.

Un JWT tiene tres partes separadas por puntos: **Header** (tipo y algoritmo), **Payload** (datos/claims) y **Signature** (firma digital).

```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.
eyJzdWIiOiIxMjM0IiwibmFtZSI6IkFuYSIsInJvbGUiOiJhZG1pbiIsImlhdCI6MTcwMDAwMDAwMCwiZXhwIjoxNzAwMDg2NDAwfQ.
SflKxwRJSMeKKF2QT4fwpMeJf36POk6yJV_adQssw5c
```

## Implementación completa con Express

```javascript
const jwt = require('jsonwebtoken');
const bcrypt = require('bcrypt');

const JWT_SECRET = process.env.JWT_SECRET; // Siempre desde variables de entorno
const JWT_REFRESH_SECRET = process.env.JWT_REFRESH_SECRET;

// Generar tokens
function generateTokens(user) {
  const payload = {
    sub: user.id,
    email: user.email,
    role: user.role
  };

  const accessToken = jwt.sign(payload, JWT_SECRET, { expiresIn: '15m' });
  const refreshToken = jwt.sign({ sub: user.id }, JWT_REFRESH_SECRET, { expiresIn: '7d' });

  return { accessToken, refreshToken };
}

// Ruta de login
app.post('/auth/login', async (req, res) => {
  const { email, password } = req.body;

  const user = await User.findByEmail(email);
  if (!user) {
    return res.status(401).json({ error: 'Credenciales inválidas' });
  }

  const isValid = await bcrypt.compare(password, user.passwordHash);
  if (!isValid) {
    return res.status(401).json({ error: 'Credenciales inválidas' });
  }

  const { accessToken, refreshToken } = generateTokens(user);

  // Guardar refresh token en base de datos (para poder invalidarlo)
  await RefreshToken.create({ userId: user.id, token: refreshToken });

  // Access token en respuesta, refresh token en cookie httpOnly
  res.cookie('refreshToken', refreshToken, {
    httpOnly: true,
    secure: process.env.NODE_ENV === 'production',
    sameSite: 'strict',
    maxAge: 7 * 24 * 60 * 60 * 1000 // 7 días
  });

  res.json({ accessToken, user: { id: user.id, email: user.email, role: user.role } });
});

// Middleware de autenticación
const authenticate = async (req, res, next) => {
  const token = req.headers.authorization?.split(' ')[1];

  if (!token) {
    return res.status(401).json({ error: 'Token requerido' });
  }

  try {
    const decoded = jwt.verify(token, JWT_SECRET);
    req.user = decoded;
    next();
  } catch (error) {
    if (error.name === 'TokenExpiredError') {
      return res.status(401).json({ error: 'Token expirado', code: 'TOKEN_EXPIRED' });
    }
    return res.status(401).json({ error: 'Token inválido' });
  }
};

// Middleware de autorización por rol
const authorize = (...roles) => (req, res, next) => {
  if (!roles.includes(req.user.role)) {
    return res.status(403).json({ error: 'Acceso denegado' });
  }
  next();
};

// Ruta de refresh token
app.post('/auth/refresh', async (req, res) => {
  const refreshToken = req.cookies.refreshToken;

  if (!refreshToken) {
    return res.status(401).json({ error: 'Refresh token requerido' });
  }

  try {
    const decoded = jwt.verify(refreshToken, JWT_REFRESH_SECRET);

    // Verificar que el refresh token existe en DB (no fue revocado)
    const storedToken = await RefreshToken.findOne({
      userId: decoded.sub,
      token: refreshToken
    });

    if (!storedToken) {
      return res.status(401).json({ error: 'Refresh token inválido' });
    }

    const user = await User.findById(decoded.sub);
    const { accessToken, refreshToken: newRefreshToken } = generateTokens(user);

    // Rotar el refresh token (invalidar el viejo)
    await RefreshToken.deleteOne({ token: refreshToken });
    await RefreshToken.create({ userId: user.id, token: newRefreshToken });

    res.cookie('refreshToken', newRefreshToken, { httpOnly: true, secure: true });
    res.json({ accessToken });

  } catch (error) {
    res.status(401).json({ error: 'Refresh token expirado o inválido' });
  }
});

// Uso de middlewares
app.get('/profile', authenticate, (req, res) => res.json(req.user));
app.delete('/users/:id', authenticate, authorize('admin'), deleteUser);
```

**Consejo de entrevista**: Los puntos clave de seguridad son: usar un `JWT_SECRET` fuerte desde variables de entorno (nunca hardcodeado), mantener el access token con tiempo de expiración corto (15-30 min), almacenar el refresh token en cookie httpOnly (no accesible desde JavaScript), e implementar rotación de refresh tokens para detectar robos.
