---
title: ¿Qué anti-patrones ves al abusar de `watch` para derivar estado o coordinar lógica entre componentes?
difficulty: hard
tags: [watchers, reactivity, components]
summary: "Pregunta senior de Vue sobre watchers y reactividad: criterios de diseño, errores comunes y decisiones de implementación."
lastReviewed: 2026-04-09
---

Aquí lo relevante en "Qué anti-patrones ves al abusar de `watch` para derivar estado o coordinar lógica entre componentes" es si sabes decidir quién posee el dato, quién lo deriva y quién reacciona fuera del componente.

## Qué evalúa el entrevistador
- Si distingues fuente de verdad, estado derivado y efecto externo sin mezclar esos papeles.
- Si sabes cuándo un valor debe calcularse, observarse o simplemente almacenarse con una referencia clara.
- Si puedes explicar el coste de cada decisión reactiva antes de que aparezca el bug.

## Respuesta sólida
- Empieza nombrando qué dato manda y qué datos pueden derivarse de él.
- Usa los mecanismos reactivos para el papel correcto y revisa cualquier duplicidad que parezca “más cómoda”.
- Si hay efectos externos, añade limpieza, cancelación y reglas de escritura visibles.

## Compromisos y errores comunes
- Usar `watch` para derivar estado que cabe en `computed` multiplica acoplamientos y desajustes.
- Perder la referencia reactiva al copiar o destructurar datos suele explicar pantallas con información vieja.

## Ejemplo de código
```vue
<script setup lang="ts">
import { computed, ref } from "vue";

const query = ref("");
const products = ref(["Vue", "Pinia", "Vitest"]);
const filtered = computed(() =>
  products.value.filter((item) => item.toLowerCase().includes(query.value.toLowerCase())),
);
</script>
```

## Ejemplo o caso real
Un caso claro aparece en una feature donde cada cambio de estado dispara cadenas de watchers y nadie sabe cuál es el origen del valor final. Si "Qué anti-patrones ves al abusar de `watch` para derivar estado o coordinar lógica entre componentes" está mal resuelto, el siguiente cambio pequeño acaba tocando más partes de las necesarias o dejando un comportamiento difícil de depurar.

## Frase corta de entrevista
> Si no sé quién posee el dato, sé que acabaré persiguiendo bugs de reactividad.
