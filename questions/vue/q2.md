---
title: ¿Cómo decidirías entre Composition API, composables, Pinia y provide/inject en una app Vue grande?
difficulty: hard
tags: [architecture, composables, pinia]
summary: "¿Cómo decidirías entre Composition API, composables, Pinia y provide/inject en una app Vue grande? en Vue senior: explicación técnica directa, decisiones de..."
lastReviewed: 2026-04-09
---

## Respuesta
- Usa Composition API para ordenar la lógica dentro del componente sin decidir todavía el alcance del estado.
- Extrae composables cuando el comportamiento se repite y puede expresarse con entradas y salidas claras; usa `provide/inject` cuando el contexto pertenece a un subárbol concreto.
- Reserva Pinia para estado compartido entre vistas, rutas o flujos largos donde compensa tener una fuente común y visible.

## Puntos clave
- La decisión correcta en Vue casi siempre depende del alcance del problema: componente, subárbol, feature o aplicación completa.
- Una buena arquitectura deja dependencias visibles, reduce puntos de escritura y mantiene cerca del caso de uso lo que cambia junto.
- Si una solución necesita demasiadas capas para explicar algo pequeño, probablemente está modelando peor el problema, no mejor.

## Errores comunes
- Crear una capa “shared” enorme donde cabe todo acaba destruyendo justo el aislamiento que se buscaba.
- Escoger mecanismo por moda en vez de por alcance suele dejar soluciones más difíciles de leer que de cambiar.

## Ejemplo de código
```ts
export const useCartStore = defineStore("cart", {
  state: () => ({ items: [] as CartItem[], pending: false }),
  getters: {
    total: (state) => state.items.reduce((sum, item) => sum + item.price * item.qty, 0),
  },
  actions: {
    add(item: CartItem) {
      this.items.push(item);
    },
  },
});
```

## Ejemplo o caso real
Esto suele aparecer en features vivas del producto donde cada cambio pequeño empieza a tocar demasiados componentes, composables o stores.

## Idea clave
> La mejor arquitectura en Vue es la que reduce dependencias hoy y sigue siendo explicable mañana.
