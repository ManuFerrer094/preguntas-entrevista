---
title: ¿Cómo modelarías estados asíncronos, cancelación y condiciones de carrera dentro de un composable?
difficulty: hard
tags: [async, reactivity, watchers]
summary: "Pregunta senior de Vue sobre asincronía y reactividad: criterios de diseño, errores comunes y decisiones de implementación."
lastReviewed: 2026-04-09
---

En "Cómo modelarías estados asíncronos, cancelación y condiciones de carrera dentro de un composable" se ve rápido si entiendes la reactividad como un modelo mental o solo como un menú de APIs.

## Qué evalúa el entrevistador
- Si distingues fuente de verdad, estado derivado y efecto externo sin mezclar esos papeles.
- Si sabes cuándo un valor debe calcularse, observarse o simplemente almacenarse con una referencia clara.
- Si puedes explicar el coste de cada decisión reactiva antes de que aparezca el bug.

## Respuesta sólida
- Empieza nombrando qué dato manda y qué datos pueden derivarse de él.
- Usa los mecanismos reactivos para el papel correcto y revisa cualquier duplicidad que parezca “más cómoda”.
- Si hay efectos externos, añade limpieza, cancelación y reglas de escritura visibles.

## Compromisos y errores comunes
- Usar `watch` para derivar estado que cabe en `computed` multiplica acoplamientos y desajustes.
- Perder la referencia reactiva al copiar o destructurar datos suele explicar pantallas con información vieja.

## Ejemplo de código
```ts
export function useUserSearch() {
  const query = ref("");
  const results = ref<User[]>([]);

  watch(query, async (value, _, onCleanup) => {
    const controller = new AbortController();
    onCleanup(() => controller.abort());
    results.value = await fetchUsers(value, controller.signal);
  });
}
```

## Ejemplo o caso real
Un caso claro aparece en un autocompletado que recibe entradas rápidas y varias respuestas llegan fuera de orden. Si "Cómo modelarías estados asíncronos, cancelación y condiciones de carrera dentro de un composable" está mal resuelto, el siguiente cambio pequeño acaba tocando más partes de las necesarias o dejando un comportamiento difícil de depurar.

## Frase corta de entrevista
> En Vue no mezclo derivación y efecto: primero calculo, luego sincronizo.
