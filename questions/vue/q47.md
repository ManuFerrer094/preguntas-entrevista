---
title: El backend es lento y no puedes cambiarlo esta sprint: ¿cómo mejorarías la UX desde Vue/Nuxt?
difficulty: medium
tags: [performance, ui, resilience]
summary: "Pregunta senior de Vue sobre rendimiento y UX: criterios de diseño, errores comunes y decisiones de implementación."
lastReviewed: 2026-04-09
---

En "El backend es lento y no puedes cambiarlo esta sprint: ¿cómo mejorarías la UX desde Vue/Nuxt" una respuesta senior empieza midiendo y termina simplificando el trabajo que nunca debió ejecutarse.

## Qué evalúa el entrevistador
- Si mides antes de optimizar y sabes distinguir coste de render, red, memoria o bundle.
- Si eliges la palanca de mejora que aporta más valor con menos complejidad adicional.
- Si puedes relacionar la optimización con una métrica visible para usuario o equipo.

## Respuesta sólida
- Reproduce el cuello de botella y decide si el problema está en render, carga, serialización o trabajo que nunca debió dispararse.
- Ataca primero las optimizaciones baratas que mantienen el código entendible: división de código, listas virtuales, carga diferida y cálculos derivados correctos.
- Deja siempre una forma de verificar la mejora para no convertir la optimización en fe.

## Compromisos y errores comunes
- Optimizar sin una señal clara suele mover complejidad sin tocar el punto caro de verdad.
- Memorizar o conservar componentes porque sí acaba siendo tan costoso como volver a renderizarlos.

## Ejemplo de código
```vue
<template>
  <OrdersSkeleton v-if="pending && !data" />
  <OrdersTable v-else :rows="data" />
  <InlineRefreshIndicator v-if="pending && data" />
  <ErrorState v-if="error" @retry="refresh" />
</template>
```

## Ejemplo o caso real
Un caso claro aparece en una pantalla operativa que tarda varios segundos, pero donde el usuario necesita seguir avanzando sin perder contexto. Si "El backend es lento y no puedes cambiarlo esta sprint: ¿cómo mejorarías la UX desde Vue/Nuxt" está mal resuelto, el siguiente cambio pequeño acaba tocando más partes de las necesarias o dejando un comportamiento difícil de depurar.

## Frase corta de entrevista
> El mejor rendimiento suele empezar quitando trabajo, no añadiendo magia.
