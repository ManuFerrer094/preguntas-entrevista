---
title: El backend es lento y no puedes cambiarlo esta sprint: ¿cómo mejorarías la UX desde Vue/Nuxt?
difficulty: medium
tags: [performance, ui, resilience]
summary: "El backend es lento y no puedes cambiarlo esta sprint: ¿cómo mejorarías la UX desde Vue/Nuxt? en Vue senior: explicación técnica directa, decisiones de diseñ..."
lastReviewed: 2026-04-09
---

## Respuesta
- Enseña estructura y datos parciales cuanto antes con skeletons, placeholders o streaming, en lugar de bloquear toda la pantalla detrás de un spinner.
- Conserva datos previos útiles mientras refrescas en segundo plano y bloquea solo las acciones que de verdad no pueden continuar.
- Aplica prefetch, invalidación selectiva y, cuando el caso lo permita, respuestas optimistas o acciones diferidas para mejorar percepción de velocidad.

## Puntos clave
- En Vue las mejoras de rendimiento más rentables suelen venir de quitar trabajo: menos renderizados, menos datos, menos bundle o menos efectos activos.
- Antes de optimizar hay que observar si el coste real está en render, red, serialización, hidratación o memoria; no todas las lentitudes se arreglan igual.
- Las técnicas como `keep-alive`, listas virtuales o componentes asíncronos ayudan cuando el cuello de botella es real; aplicarlas por costumbre añade complejidad.

## Errores comunes
- Optimizar sin medir suele desplazar complejidad hacia el código sin tocar el cuello de botella real.
- Conservar o memorizar componentes por costumbre puede costar más que volver a renderizarlos.

## Ejemplo de código
```vue
<template>
  <OrdersSkeleton v-if="pending && !data" />
  <OrdersTable v-else :rows="data" />
  <InlineRefreshIndicator v-if="pending && data" />
  <ErrorState v-if="error" @retry="refresh" />
</template>
```

## Ejemplo o caso real
Suele aparecer en vistas con tablas, paneles laterales, gráficos o rutas pesadas donde el usuario percibe lentitud aunque el problema técnico real no esté donde parecía.

## Idea clave
> Rendimiento no es acumular técnicas; es quitar trabajo innecesario donde realmente duele.
