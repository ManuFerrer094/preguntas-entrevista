---
title: ¿Cuándo un composable reutilizable mejora el diseño y cuándo solo se convierte en una bolsa de efectos secundarios?
difficulty: medium
tags: [composables, effects, architecture]
summary: "Pregunta senior de Vue sobre composables y efectos: criterios de diseño, errores comunes y decisiones de implementación."
lastReviewed: 2026-04-09
---

Esta pregunta separa a quien conoce el ecosistema Vue de quien sabe usarlo para resolver "Cuándo un composable reutilizable mejora el diseño y cuándo solo se convierte en una bolsa de efectos secundarios" sin añadir acoplamiento innecesario.

## Qué evalúa el entrevistador
- Si conviertes una preferencia de organización en criterios visibles de alcance, dependencia y coste de cambio.
- Si sabes separar lo que debería quedarse local de lo que merece convertirse en contrato estable para varias pantallas.
- Si justificas la decisión por claridad operativa del sistema y no por seguir la moda del ecosistema.

## Respuesta sólida
- Define primero el alcance natural del problema: componente, subárbol, feature, ruta o aplicación completa.
- Haz explícitas las dependencias relevantes y deja fuera de la solución todo lo que aún no aporte una mejora clara.
- Comprueba que el resultado deja un flujo legible para quien mantenga la funcionalidad dentro de seis meses.

## Compromisos y errores comunes
- Crear capas o patrones por adelantado suele esconder el problema real sin reducir acoplamiento.
- Cuando todo acaba en utilidades globales o piezas “compartidas”, la estructura deja de ayudar y solo añade fricción.

## Ejemplo de código
```ts
export function useUserSearch() {
  const query = ref("");
  const results = ref<User[]>([]);

  watch(query, async (value, _, onCleanup) => {
    const controller = new AbortController();
    onCleanup(() => controller.abort());
    results.value = await fetchUsers(value, controller.signal);
  });
}
```

## Ejemplo o caso real
Un caso claro aparece en un buscador que ya mezcla debounce, URL, red, analytics y permisos dentro del mismo `useX`. Si "Cuándo un composable reutilizable mejora el diseño y cuándo solo se convierte en una bolsa de efectos secundarios" está mal resuelto, el siguiente cambio pequeño acaba tocando más partes de las necesarias o dejando un comportamiento difícil de depurar.

## Frase corta de entrevista
> En Vue prefiero una arquitectura entendible a una arquitectura vistosa.
