---
title: ¿Cuándo usarías guards de Vue Router, middleware de Nuxt o fetching en la pantalla para preparar una ruta?
difficulty: medium
tags: [routing, ssr, navigation]
summary: "Pregunta senior de Vue sobre Vue Router y SSR: criterios de diseño, errores comunes y decisiones de implementación."
lastReviewed: 2026-04-09
---

Esta pregunta comprueba si tratas el router como contrato de navegación y no como cajón de lógica transversal.

## Qué evalúa el entrevistador
- Si ordenas navegación, permisos, carga de datos y división de código como un sistema coherente.
- Si sabes cuándo una decisión pertenece al router y cuándo debería vivir en la pantalla o en el servidor.
- Si mantienes rutas predecibles incluso cuando la aplicación crece por áreas de negocio.

## Respuesta sólida
- Piensa el router como contrato de navegación, no como lugar donde esconder cualquier preocupación transversal.
- Usa guards, middleware o carga de datos según el momento en que necesitas decidir o bloquear la navegación.
- Mantén carga diferida y metadatos cerca de la feature que representan para que el mapa de rutas siga siendo entendible.

## Compromisos y errores comunes
- Poner demasiada lógica de negocio en guards y middleware vuelve opaca la navegación y complica depuración.
- Disparar la misma carga desde router, componente y store suele terminar en peticiones duplicadas y estados inciertos.

## Ejemplo de código
```ts
export default defineNuxtRouteMiddleware((to) => {
  const session = useSessionStore();
  if (to.meta.requiresAuth && !session.isAuthenticated) {
    return navigateTo({ path: "/login", query: { redirect: to.fullPath } });
  }
});
```

## Ejemplo o caso real
Un caso claro aparece en una app híbrida donde unas decisiones bloquean la entrada y otras solo preparan datos o contexto. Si "Cuándo usarías guards de Vue Router, middleware de Nuxt o fetching en la pantalla para preparar una ruta" está mal resuelto, el siguiente cambio pequeño acaba tocando más partes de las necesarias o dejando un comportamiento difícil de depurar.

## Frase corta de entrevista
> Cuando una ruta necesita demasiadas excepciones, reviso antes el diseño que el guard.
