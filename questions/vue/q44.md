---
title: Un `watch` dispara llamadas duplicadas al backend: ¿cómo lo depurarías?
difficulty: hard
tags: [debugging, watchers, components]
summary: "Pregunta senior de Vue sobre depuración y watchers: criterios de diseño, errores comunes y decisiones de implementación."
lastReviewed: 2026-04-09
---

La respuesta buena en "Un `watch` dispara llamadas duplicadas al backend: ¿cómo lo depurarías" tiene método: reducir el problema, observar el flujo y corregir la causa real.

## Qué evalúa el entrevistador
- Si reduces el problema antes de corregirlo y sabes observar qué dispara cada cambio relevante.
- Si revisas orden de eventos, ciclo de vida, peticiones en vuelo y conservación de componentes con método.
- Si dejas una forma de prevenir la recaída después de arreglar el bug.

## Respuesta sólida
- Haz visible el flujo con logs, herramientas, tests o pasos de reproducción mínimos antes de tocar diseño.
- Aísla si el fallo viene de lectura, escritura, conservación de estado o asincronía compitiendo por el último valor.
- Corrige la causa y deja una prueba, una métrica o una simplificación que evite repetir la misma clase de error.

## Compromisos y errores comunes
- Cambiar varias cosas a la vez puede “arreglar” el síntoma pero deja intacta la causa real.
- Sin limpieza, cancelación o reglas claras de activación, los bugs reactivos vuelven con otra forma.

## Ejemplo de código
```ts
export function useUserSearch() {
  const query = ref("");
  const results = ref<User[]>([]);

  watch(query, async (value, _, onCleanup) => {
    const controller = new AbortController();
    onCleanup(() => controller.abort());
    results.value = await fetchUsers(value, controller.signal);
  });
}
```

## Ejemplo o caso real
Un caso claro aparece en un autocompletado o filtro donde montaje, cambios de ruta y entrada del usuario compiten por lanzar fetches. Si "Un `watch` dispara llamadas duplicadas al backend: ¿cómo lo depurarías" está mal resuelto, el siguiente cambio pequeño acaba tocando más partes de las necesarias o dejando un comportamiento difícil de depurar.

## Frase corta de entrevista
> Depuro recortando el problema hasta que solo quede una causa creíble.
