---
title: ¿Qué estrategia seguirías para optimistic UI, rollback e invalidación de datos en Pinia o composables?
difficulty: hard
tags: [state-management, pinia, resilience]
summary: "Pregunta senior de Vue sobre gestión de estado y Pinia: criterios de diseño, errores comunes y decisiones de implementación."
lastReviewed: 2026-04-09
---

En "Qué estrategia seguirías para optimistic UI, rollback e invalidación de datos en Pinia o composables" se ve si sabes repartir estado local, compartido y remoto sin duplicaciones cómodas pero caras de mantener.

## Qué evalúa el entrevistador
- Si sabes decidir qué estado debe ser local, compartido o remoto sin duplicarlo en varias capas.
- Si modelas acciones, lecturas y escrituras con una fuente de verdad clara.
- Si piensas en consistencia y trazabilidad antes de hablar de la librería.

## Respuesta sólida
- Declara quién posee el dato y qué piezas solo lo observan o lo transforman.
- Conserva en Pinia solo lo que de verdad cruza pantallas, sesiones o flujos relevantes.
- Para asincronía, define cómo cancelas, invalidas, rehaces o reviertes sin esconderlo en watchers sueltos.

## Compromisos y errores comunes
- Duplicar información entre formulario, URL, caché y store hace muy difícil saber qué valor es el bueno.
- Llevar a estado global una necesidad temporal o local vuelve más opaca la aplicación sin resolver el diseño.

## Ejemplo de código
```ts
async function toggleDone(taskId: string) {
  const previous = tasks.value.slice();
  tasks.value = tasks.value.map((task) =>
    task.id === taskId ? { ...task, done: !task.done } : task,
  );

  try {
    await api.patch(`/tasks/${taskId}`);
  } catch (error) {
    tasks.value = previous;
    throw error;
  }
}
```

## Ejemplo o caso real
Un caso claro aparece en una lista de tareas donde el usuario espera ver el cambio instantáneo aunque la API falle a veces. Si "Qué estrategia seguirías para optimistic UI, rollback e invalidación de datos en Pinia o composables" está mal resuelto, el siguiente cambio pequeño acaba tocando más partes de las necesarias o dejando un comportamiento difícil de depurar.

## Frase corta de entrevista
> Una sola fuente de verdad vale más que tres sincronizaciones “cómodas”.
