---
title: ¿Cuándo cargarías datos en la ruta, en el componente, en Pinia o en el servidor con Nuxt?
difficulty: hard
tags: [state-management, routing, ssr]
summary: "Pregunta senior de Vue sobre gestión de estado y Vue Router: criterios de diseño, errores comunes y decisiones de implementación."
lastReviewed: 2026-04-09
---

La calidad de la respuesta en "Cuándo cargarías datos en la ruta, en el componente, en Pinia o en el servidor con Nuxt" depende de si conviertes el estado en una fuente de verdad clara y trazable.

## Qué evalúa el entrevistador
- Si sabes decidir qué estado debe ser local, compartido o remoto sin duplicarlo en varias capas.
- Si modelas acciones, lecturas y escrituras con una fuente de verdad clara.
- Si piensas en consistencia y trazabilidad antes de hablar de la librería.

## Respuesta sólida
- Declara quién posee el dato y qué piezas solo lo observan o lo transforman.
- Conserva en Pinia solo lo que de verdad cruza pantallas, sesiones o flujos relevantes.
- Para asincronía, define cómo cancelas, invalidas, rehaces o reviertes sin esconderlo en watchers sueltos.

## Compromisos y errores comunes
- Duplicar información entre formulario, URL, caché y store hace muy difícil saber qué valor es el bueno.
- Llevar a estado global una necesidad temporal o local vuelve más opaca la aplicación sin resolver el diseño.

## Ejemplo de código
```vue
<script setup lang="ts">
const route = useRoute();
const { data, pending, error, refresh } = await useAsyncData(
  () => `product-${route.params.id}`,
  () => $fetch(`/api/products/${route.params.id}`),
  { watch: [() => route.params.id] },
);
</script>
```

## Ejemplo o caso real
Un caso claro aparece en una vista de producto que combina SEO, navegación rápida y refresco después de acciones del usuario. Si "Cuándo cargarías datos en la ruta, en el componente, en Pinia o en el servidor con Nuxt" está mal resuelto, el siguiente cambio pequeño acaba tocando más partes de las necesarias o dejando un comportamiento difícil de depurar.

## Frase corta de entrevista
> El estado global es caro: solo lo pago cuando la aplicación realmente lo necesita.
