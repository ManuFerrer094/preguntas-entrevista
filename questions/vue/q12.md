---
title: ¿Qué papel tienen `effectScope`, `onScopeDispose` y la limpieza de efectos en composables complejos?
difficulty: hard
tags: [reactivity, effects, watchers]
summary: "Pregunta senior de Vue sobre reactividad y efectos: criterios de diseño, errores comunes y decisiones de implementación."
lastReviewed: 2026-04-09
---

En "Qué papel tienen `effectScope`, `onScopeDispose` y la limpieza de efectos en composables complejos" se ve rápido si entiendes la reactividad como un modelo mental o solo como un menú de APIs.

## Qué evalúa el entrevistador
- Si distingues fuente de verdad, estado derivado y efecto externo sin mezclar esos papeles.
- Si sabes cuándo un valor debe calcularse, observarse o simplemente almacenarse con una referencia clara.
- Si puedes explicar el coste de cada decisión reactiva antes de que aparezca el bug.

## Respuesta sólida
- Empieza nombrando qué dato manda y qué datos pueden derivarse de él.
- Usa los mecanismos reactivos para el papel correcto y revisa cualquier duplicidad que parezca “más cómoda”.
- Si hay efectos externos, añade limpieza, cancelación y reglas de escritura visibles.

## Compromisos y errores comunes
- Usar `watch` para derivar estado que cabe en `computed` multiplica acoplamientos y desajustes.
- Perder la referencia reactiva al copiar o destructurar datos suele explicar pantallas con información vieja.

## Ejemplo de código
```ts
export function useLiveOrders() {
  const scope = effectScope();

  scope.run(() => {
    const socket = connectOrdersStream();
    onScopeDispose(() => socket.close());
  });

  return () => scope.stop();
}
```

## Ejemplo o caso real
Un caso claro aparece en un composable que abre sockets, listeners y temporizadores y se reutiliza en varias vistas. Si "Qué papel tienen `effectScope`, `onScopeDispose` y la limpieza de efectos en composables complejos" está mal resuelto, el siguiente cambio pequeño acaba tocando más partes de las necesarias o dejando un comportamiento difícil de depurar.

## Frase corta de entrevista
> En Vue no mezclo derivación y efecto: primero calculo, luego sincronizo.
