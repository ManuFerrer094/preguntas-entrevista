---
title: ¿Cómo decidirías entre `v-if`, `v-show`, componentes dinámicos, `keep-alive` y `Suspense` en un caso real?
difficulty: medium
tags: [performance, components, suspense]
summary: "Pregunta senior de Vue sobre rendimiento y componentes: criterios de diseño, errores comunes y decisiones de implementación."
lastReviewed: 2026-04-09
---

Aquí interesa menos recitar técnicas y más demostrar que sabes localizar el trabajo caro antes de optimizar.

## Qué evalúa el entrevistador
- Si mides antes de optimizar y sabes distinguir coste de render, red, memoria o bundle.
- Si eliges la palanca de mejora que aporta más valor con menos complejidad adicional.
- Si puedes relacionar la optimización con una métrica visible para usuario o equipo.

## Respuesta sólida
- Reproduce el cuello de botella y decide si el problema está en render, carga, serialización o trabajo que nunca debió dispararse.
- Ataca primero las optimizaciones baratas que mantienen el código entendible: división de código, listas virtuales, carga diferida y cálculos derivados correctos.
- Deja siempre una forma de verificar la mejora para no convertir la optimización en fe.

## Compromisos y errores comunes
- Optimizar sin una señal clara suele mover complejidad sin tocar el punto caro de verdad.
- Memorizar o conservar componentes porque sí acaba siendo tan costoso como volver a renderizarlos.

## Ejemplo de código
```vue
<template>
  <KeepAlive include="SearchView,ResultsView">
    <component :is="currentView" v-if="ready" />
  </KeepAlive>
  <Spinner v-else-if="loading" />
  <p v-else>No hay datos.</p>
</template>
```

## Ejemplo o caso real
Un caso claro aparece en una interfaz con pestañas, paneles secundarios y cargas lentas que no deberían bloquear toda la vista. Si "Cómo decidirías entre `v-if`, `v-show`, componentes dinámicos, `keep-alive` y `Suspense` en un caso real" está mal resuelto, el siguiente cambio pequeño acaba tocando más partes de las necesarias o dejando un comportamiento difícil de depurar.

## Frase corta de entrevista
> Mido primero y simplifico después; optimizar a ciegas rara vez sale barato.
