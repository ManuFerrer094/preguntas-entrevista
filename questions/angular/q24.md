---
title: ¿Cómo optimizarías una lista grande con `@for`, `track`/`trackBy` y virtual scrolling?
difficulty: hard
tags: [performance, rendering, virtual-scrolling]
summary: "¿Cómo optimizarías una lista grande con `@for`, `track`/`trackBy` y virtual scrolling? en Angular: criterios sobre rendimiento y renderizado, errores comunes..."
lastReviewed: 2026-04-04
---

La mejor forma de responder "¿Cómo optimizarías una lista grande con `@for`, `track`/`trackBy` y virtual scrolling?" en Angular es separar mecanismo técnico, criterio de uso y señales de revisión.

Una respuesta senior se nota cuando nombras qué riesgo quieres reducir con rendimiento en Angular para "Cómo optimizarías una lista grande con `@for`, `track`/`trackBy` y virtual scrolling", qué concesión aceptarías frente a renderizado y qué comprobarías antes de extender la decisión a todo el sistema.

## Qué evalúa el entrevistador
- Si distingues qué parte de "Cómo optimizarías una lista grande con `@for`, `track`/`trackBy` y virtual scrolling" pertenece a rendimiento y cuál debería resolverse en renderizado.
- Si conviertes la respuesta en criterios observables: límites claros, impacto en el mantenimiento y forma de detectar regresiones.
- Si entiendes qué dispara trabajo real de render o hidratación y cuándo merece la pena optimizar frente a cuándo solo estás moviendo complejidad.

## Respuesta sólida
- Explica qué unidad quieres volver a pintar, conservar o diferir y por qué esa decisión mejora la experiencia sin complicar el árbol.
- Relaciona la solución con claves, memoización, detección de cambios, hidratación o virtualización solo si el cuello de botella está realmente ahí.
- Si propones optimización, acompáñala de una forma de medirla con herramientas o métricas visibles.

## Compromisos y errores comunes
- Optimizar sin perfilar antes suele desplazar la complejidad hacia el componente sin tocar el verdadero cuello de botella.
- Forzar memoización, cachés o control fino del render donde no hace falta complica la depuración y suele envejecer mal.

## Ejemplo de código
Un ejemplo pequeño ayuda a ver dónde colocarías la lógica de rendimiento en "Cómo optimizarías una lista grande con `@for`, `track`/`trackBy` y virtual scrolling" y qué parte dejarías derivada o encapsulada.

```ts
import { ChangeDetectionStrategy, Component, computed, signal } from '@angular/core';

@Component({
  selector: 'app-product-filter',
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  template: `
    <input [value]="query()" (input)="query.set(($any($event.target)).value)" />
    <ul>
      @for (product of filteredProducts(); track product.id) {
        <li>{{ product.name }}</li>
      }
    </ul>
  `,
})
export class ProductFilterComponent {
  readonly query = signal('');
  readonly products = signal([{ id: 1, name: 'Angular' }, { id: 2, name: 'RxJS' }]);
  readonly filteredProducts = computed(() =>
    this.products().filter((product) =>
      product.name.toLowerCase().includes(this.query().trim().toLowerCase()),
    ),
  );
}
```

Lo importante no es la API concreta, sino que la solución hace visible la fuente de verdad, el tratamiento del error y el punto exacto donde rendimiento se sincroniza con renderizado dentro de "Cómo optimizarías una lista grande con `@for`, `track`/`trackBy` y virtual scrolling" en Angular.

## Ejemplo o caso real
Un caso creíble para "¿Cómo optimizarías una lista grande con `@for`, `track`/`trackBy` y virtual scrolling?" aparece cuando una funcionalidad de Angular mezcla rendimiento con renderizado y el equipo empieza a tocar demasiados puntos para un cambio pequeño. Ahí conviene probar la solución sobre una pantalla o flujo acotado, medir si reduce fricción y solo después extender el patrón.

## Frase corta de entrevista
> En "Cómo optimizarías una lista grande con `@for`, `track`/`trackBy` y virtual scrolling" me interesa más mantener una fuente de verdad clara y una validación honesta que sonar sofisticado.
