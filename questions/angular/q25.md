---
title: ¿Cuándo merece la pena salir de Zone.js o usar zoneless y qué cambia en el modelo mental?
difficulty: hard
tags: [performance, zoneless, change-detection]
summary: "¿Cuándo merece la pena salir de Zone.js o usar zoneless y qué cambia en el modelo mental? en Angular: criterios sobre rendimiento y sin Zone.js, errores comu..."
lastReviewed: 2026-04-04
---

Detrás de "¿Cuándo merece la pena salir de Zone.js o usar zoneless y qué cambia en el modelo mental?" suele haber una tensión real en Angular entre rendimiento y sin Zone.js.

En una entrevista fuerte gana peso la persona que habla de costes, señales de degradación, deuda aceptada y plan de validación para "Cuándo merece la pena salir de Zone.js o usar zoneless y qué cambia en el modelo mental", no solo de API o sintaxis.

## Qué evalúa el entrevistador
- Si distingues qué parte de "Cuándo merece la pena salir de Zone.js o usar zoneless y qué cambia en el modelo mental" pertenece a rendimiento y cuál debería resolverse en sin Zone.js.
- Si conviertes la respuesta en criterios observables: límites claros, impacto en el mantenimiento y forma de detectar regresiones.
- Si entiendes qué dispara trabajo real de render o hidratación y cuándo merece la pena optimizar frente a cuándo solo estás moviendo complejidad.

## Respuesta sólida
- Explica qué unidad quieres volver a pintar, conservar o diferir y por qué esa decisión mejora la experiencia sin complicar el árbol.
- Relaciona la solución con claves, memoización, detección de cambios, hidratación o virtualización solo si el cuello de botella está realmente ahí.
- Si propones optimización, acompáñala de una forma de medirla con herramientas o métricas visibles.

## Compromisos y errores comunes
- Optimizar sin perfilar antes suele desplazar la complejidad hacia el componente sin tocar el verdadero cuello de botella.
- Forzar memoización, cachés o control fino del render donde no hace falta complica la depuración y suele envejecer mal.

## Ejemplo de código
Este fragmento sirve para bajar "Cuándo merece la pena salir de Zone.js o usar zoneless y qué cambia en el modelo mental" a código ejecutable y mostrar qué decisiones conviene hacer explícitas cuando rendimiento empieza a cruzarse con sin Zone.js.

```ts
import { ChangeDetectionStrategy, Component, computed, signal } from '@angular/core';

@Component({
  selector: 'app-product-filter',
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  template: `
    <input [value]="query()" (input)="query.set(($any($event.target)).value)" />
    <ul>
      @for (product of filteredProducts(); track product.id) {
        <li>{{ product.name }}</li>
      }
    </ul>
  `,
})
export class ProductFilterComponent {
  readonly query = signal('');
  readonly products = signal([{ id: 1, name: 'Angular' }, { id: 2, name: 'RxJS' }]);
  readonly filteredProducts = computed(() =>
    this.products().filter((product) =>
      product.name.toLowerCase().includes(this.query().trim().toLowerCase()),
    ),
  );
}
```

Fíjate en que el ejemplo deja claras las fronteras de "Cuándo merece la pena salir de Zone.js o usar zoneless y qué cambia en el modelo mental", nombra los estados relevantes y evita trabajo implícito que luego cuesta depurar.

## Ejemplo o caso real
Un caso creíble para "¿Cuándo merece la pena salir de Zone.js o usar zoneless y qué cambia en el modelo mental?" aparece cuando una funcionalidad de Angular mezcla rendimiento con sin Zone.js y el equipo empieza a tocar demasiados puntos para un cambio pequeño. Ahí conviene probar la solución sobre una pantalla o flujo acotado, medir si reduce fricción y solo después extender el patrón.

## Frase corta de entrevista
> Si una decisión de Angular no mejora claridad, coste de cambio o fiabilidad, probablemente aún no merece existir.
